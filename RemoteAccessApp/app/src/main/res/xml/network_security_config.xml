<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev-only: allow cleartext (http / ws) to LAN/localhost -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
